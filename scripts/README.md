# Development scripts

This directory is reserved for auditable, offline development tooling such as future
vector and fuzz-corpus maintenance. Release builds do not execute network scripts or
download tools. The current build entry points are the deliberately small top-level
`build.sh`, `build.ps1`, and `verify-build.sh` files.

